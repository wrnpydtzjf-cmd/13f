# Li Lu 13F Tracker

价值投资者 13F 持仓追踪 + 港美股分拆进展监控。

## 投资者配置（单一数据源）

**`investors.json` 是全部 13 位投资者的唯一配置源**（2026-07-26 重构后）。新增/修改投资者只需编辑这一个文件，无需再碰 `app.js`、`fetch_13f_all.py`、`fetch_prices_all.py`、`enrich_metadata.py` 里的任何硬编码列表。

每条投资者配置包含：`id`、`name`/`nameEn`、`cik`（无 13F 义务的投资者为 `null`，如 webb）、`manager`、`people`、`dataFile`、`pricesFile`、`hkFile`（无港股持仓为 `null`）、`market`（US/HK）、`source13F`（是否走 13F 抓取流程）、`consolidate`（是否合并多基金持仓，仅 buffett 为 true）、`inValueScreen`（是否进入价值筛选候选池，仅 webb 为 false）。

详情页面的文字介绍（“关于XX”、投资理念等 prose 内容）仍在 `app.js` 的 `updateInvestorContent()` 里手动维护——这是设计上的取舍，不是遗漏。

## 文件分类

### 源数据（手动维护，禁止 CI 覆盖）

| 文件 | 说明 |
|------|------|
| `investors.json` | **投资者配置单一数据源**，见上 |
| `data.json` | 李录 13F 持仓（含历史） |
| `pabrai_data.json` | 帕布莱 13F |
| `duan.json` | 段永平 13F |
| `tepper.json` | 大卫·泰珀 13F |
| `buffett.json` | 巴菲特 13F |
| `akre.json` | 查克·阿克雷 13F |
| `greenberg.json` | 格林伯格 13F |
| `klarman.json` / `ackman.json` / `abrams.json` / `berkowitz.json` / `hawkins.json` | 塞斯·克拉曼 / 比尔·阿克曼 / 大卫·艾布拉姆斯 / 布鲁斯·伯科威茨 / 梅森·霍金斯 13F（2026-07-26 新增） |
| `webb.json` | David Webb 港股持仓 |
| `spinoff.json` | 港股分拆公告（手动 + CI 追加） |
| `spinoff_us.json` | 美股分拆公告（KNOWN_SPINOFFS 手动维护 + CI 追加） |

### 计算结果（CI 自动写入，可安全覆盖）

| 文件 | 来源脚本 |
|------|----------|
| `prices.json` / `prices_*.json` | `fetch_prices_all.py` |
| `hk_holdings.json` / `*_hk.json` | `fetch_webb_holdings.py` / `monitor_hk_disclosures.py` |
| `metadata_cache.json` | `enrich_metadata.py` |
| `alerts_hk_persons.json` | `monitor_hk_disclosures.py` |
| `run_status.json` | `update_status.py` |

### 脚本说明

| 脚本 | 功能 |
|------|------|
| `fetch_13f_all.py` | 从 SEC EDGAR 抓取所有投资者 13F（唯一入口） |
| `fetch_prices_all.py` | 抓取所有投资者股价 + 历史均价（唯一入口） |
| `fetch_spinoff.py` | 港股分拆公告抓取 |
| `fetch_spinoff_us.py` | 美股分拆公告抓取（含 `KNOWN_SPINOFFS` 手动库） |
| `spinoff_price_refresh.py` | 刷新分拆子公司股价 + 母公司公告以来表现 |
| `fetch_webb_holdings.py` | Webb 港股持仓抓取（仅周一） |
| `enrich_metadata.py` | 补充中文名 + 行业（SiliconFlow LLM） |
| `resolve_unmapped_tickers.py` | 自动解析 `?` 前缀未识别 ticker（CUSIP → 真实 ticker，OpenFIGI API） |
| `monitor_hk_disclosures.py` | 段永平港股权益披露监控 |
| `update_status.py` | CI 运行状态记录 |

## CI 调度

- **UTC 00:00**（HKT 08:00）— 港股开盘前更新
- **UTC 13:00**（HKT 21:00）— 美股盘中更新

## 常见维护

**新公司改名 / 新 ticker**：在 `fetch_13f_all.py` 的 `TICKER_MAP` 里添加映射。CI 日志会打印 `⚠️ 未识别 ticker` 告警。

**新增美股分拆**：在 `fetch_spinoff_us.py` 的 `KNOWN_SPINOFFS` 列表里手动添加条目，填写 `distributionDate`。

**新增美股分拆已知分拆日期**：在 `spinoff_price_refresh.py` 的 `US_KNOWN_DIST_DATES` 字典里补充。

**新增投资者**（2026-07-26 重构后）：只需在 `investors.json` 里新加一条配置。若需要页面上的文字介绍，再在 `app.js` 的 `updateInvestorContent()` 里补充 prose。不要再在任何地方添加硬编码的投资者列表。

**发现新的未识别 ticker/未翻译中文名**（见下方 2026-07-26 第二条记录）：不需要手工处理。CI 会在每次运行时自动调用 `resolve_unmapped_tickers.py`（CUSIP 解析）+ `enrich_metadata.py`（LLM 翻译）。若某个 ticker/公司名长期保持未解析，大概率是已退市/被并购公司，OpenFIGI 和 SEC 都查不到，属预期行为，不需要人工干预。

## 架构决策与 bug 修复记录

### 2026-07-26：investors.json 单一数据源重构

**背景**：新增克拉曼/阿克曼/艾布拉姆斯/伯科威茨/霍金斯 5 位投资者时，因为投资者列表同时硬编码在 4 个文件里（`app.js`、`fetch_13f_all.py`、`fetch_prices_all.py`、`enrich_metadata.py`），每个地方都很容易漏改。因此重构为以 `investors.json` 为单一数据源，四个文件均改为动态读取。

**过程中发现并修复的3个隐藏 bug**（都与硬编码重复/遗漏相关，说明多处硬编码列表本质上无法保证一致性）：

1. **`fetch_prices_all.py` 首次抓取新投资者时 `UnboundLocalError` 崩溃**——导致5位新投资者的 `prices_*.json` 从未生成。修复：确保异常变量（`ex`）在使用前初始化。
2. **`app.js` 的 `loadHKHoldings()` 对无港股持仓的投资者错误 fallback 到李录的港股数据**——根因是 JS 允许同名 `function` 声明并静静覆盖前一个，当时 `app.js` 里存在两个 `renderHKHoldings()` 定义，前面修对的那个其实是死代码，实际生效的是后面未修对的那个。修复：删除重复定义，在真正生效的函数里改为从 `investors.json` 的 `hkFile` 字段读取，`null` 时显示空状态。
3. **`enrich_metadata.py` 的 `data_files`/`FILES` 硬编码列表未包含新投资者**——导致中文名/行业分类不会为新投资者自动补全。修复时附带发现并补上了之前也被遗漏的 `webb_hk.json`。

**另外发现两个错误的硬编码 CIK 值**（不是拼写错误而是指向了完全无关的公司）：`app.js` 的 `INVESTOR_CIK` 字典里 pabrai 显示 `0001474216`（实为 Franchise Portfolio 2, Inc.），akre 显示 `0001499406`（实为 KLP 2010 ANP Mirror Trust B）。均已按 [SEC EDGAR](https://data.sec.gov/submissions/CIK0001549575.json) 校验改为正确值（pabrai=`1549575`，akre=`1112520`），并不再单独硬编码，改为从 `investors.json` 派生。

**经验教训**：
- 若发现“投资者 X 被遗漏在某硬编码列表里”，要 grep 所有类似/同构列表，不能假设只有一处。
- JS 里同名 `function` 声明会被静静覆盖，要 grep 重复函数名，不能假设只存在一份定义。
- 合并硬编码配置时，要对比权威数据源（如 SEC EDGAR）逐个校验，不能只看内部一致性——内部一致不代表数值本身正确。
- CI `✓` 可能误导（容错处理会让失败步骤仍显示成功），要用 `gh run view --log` 看实际日志，不能只看 step 级状态图标。

### 2026-07-26（第二条）：CUSIP 自动解析 ticker + LLM 中文名翻译补全

**背景**：用户反馈“很多股票名没有翻译成中文”。诊断发现这其实是两个独立问题叠加：

1. **Ticker 解析层**：`fetch_13f_all.py` 的 `resolve_ticker()` 只能靠手工维护的 `TICKER_MAP`/`TICKER_CLASS_MAP`/`CUSIP_TICKER_MAP` 查表，查不到就 fallback 成 `?公司原名`存入 JSON。`enrich_metadata.py` 又会主动跳过所有 `?` 前缀 ticker（因为模糊的公司名字符串无法可靠地匹配到真实公司，避免引入错误映射）。全量扫描发现 13,961 条历史持仓记录中 59.2% 缺 cnName，其中 99.2% 跟这个 `?` 前缀问题相关。
2. **中文翻译层**（更根本的问题，规模更大）：即使 ticker 解析正确，`enrich_metadata.py` 的 `fetch_yf_info()` 只有命中手写白名单 `MANUAL_CN_NAME`（仅 ~130 个 ticker）才会得到真正中文名，否则就直接用 yfinance 的 `longName`（英文）当作 cnName 存入。实测全量数据发现 8,182 条真实 ticker 记录中，实际是英文 fallback 的有 3,103 条，**比真正翻译成中文的 2,892 条还多**——这才是用户感觉“很多没翻译”的真正根源。

**修复方案**（严格按用户要求——全自动化、优先确定性规则、不确定的不乱填）：

1. **新增 `resolve_unmapped_tickers.py`**：扫描所有投资者数据文件里 `?` 前缀记录的 CUSIP，调用 [OpenFIGI `/mapping` API](https://www.openfigi.com/api/documentation)（免费、无需 API key）批量解析。验证过的选择规则：优先取 `exchCode=='US' 且 securityType=='Common Stock'` 且候选 ticker 唯一，否则退而求其次取任意 US 交易所记录但仍要求唯一，出现歧义（多个不同 ticker）则不采纳。全量跑一次结果：541 个唯一 CUSIP 中 201 个解析成功（37%），回填 2,118 条历史记录的 ticker。解析结果持久化写入独立的 `resolved_cusip_map.json`（不直接改手写 `CUSIP_TICKER_MAP` 字面量，关注点分离，方便 CI 自动提交），`fetch_13f_all.py` 启动时自动合并进 `CUSIP_TICKER_MAP`。剩余 340 个 CUSIP（约 67%）经根因排查均为已退市/被并购/私有化公司（如二十一世纪福斯、VMware、Sears、CBS 等），OpenFIGI 和 SEC 官方 ticker 表都只追踪当前活跃登记人，无法通过规则解析，不强行猜测。
2. **`enrich_metadata.py` 新增 `translate_names_to_chinese()`**：在每次 yfinance 回填完成后，扫描缓存里所有 `cnName` 不含中文字符的条目，批量交给 SiliconFlow LLM 翻译。**关键安全设计**：要求 LLM 返回 `{"编号": "译文"}` 的 JSON 对象而不是纯数组，按编号回填而不是按数组位置 `zip`——即使 LLM 漏答/乱序部分编号，已经答对的也不会因位置错位而被错误归属到另一个公司。本地用 mock 单元测试验证了正常/漏答/乱序/异常格式 4 种场景都不会产生错误映射。翻译结果同时回写 `metadata_cache.json` 缓存和所有已落盘的数据文件。
3. **CI 集成**：`.github/workflows/update.yml` 新增 `Resolve unmapped tickers via CUSIP` 步骤，放在 `Enrich metadata` 之前执行（先解析 ticker，再让翻译步骤去查真实 ticker 的信息）。两个步骤都是幂等的（已解析过的 CUSIP/已翻译过的名字不会重复请求），每次 CI 运行都会自动处理新出现的未解析记录，无需人工重跑。
4. **删除死代码**：确认 `fetch_13f.py`/`fetch_13f_backup.py`/`fetch_13f_akre_greenberg.py`/`fetch_13f_buffett.py`/`fetch_13f_duan.py`/`fetch_13f_pabrai.py`/`fetch_13f_tepper.py`/`fetch_pabrai_prices.py` 8 个旧脚本均已被 `fetch_13f_all.py` 取代且不被任何地方引用，删除。

**经验教训**：
- 字段名叫 `cnName` 不代表里面真的是中文—— fallback 逻辑可能对 API 返回值原样不动地当成目标语言写入。评估这类字段的真实覆盖率时，要检查字符内容本身（如正则匹配中文字符），不能只看字段是否为空。
- 批量 LLM 翻译/映射任务中，用数组位置 `zip` 对应输入输出存在隐藏风险：一旦 LLM 漏答或乱序，会静默产生错位映射而不报错。要求 LLM 返回带显式编号/key 的结构化格式，按 key 回填而不是按位置回填。
- 免费 API 的限流参数要以官方文档为准，不能凭经验猜测（OpenFIGI 无 key 时实际是每请求最多 10 个 job，不是直觉以为的 25）。
- 能解决的部分用确定性规则自动化解决，解决不了的部分（数据源本身不存在权威映射）应该诚实保留原状，而不是用 LLM 猜测填充——自动化的边界应该是“能确认的都自动确认，不能确认的诚实报告无法自动化”。

### 2026-07-26（第三条）：发现并修复 history.quarters 乱序导致的图表/AI摘要错误

**背景**：本次回归验证截图时意外发现格伦伯格“历史趋势”图表 X 轴年份乱序（2025 后突然出现 2013）。根因排查确认与本次 CUSIP/翻译修复无关（`app.js` 本次唯一改动只涉及状态面板标签），是早已存在的数据序问题，因为认为“图表能看就行”而顺便一并修复。

**根因**：`fetch_13f_all.py` 的 `process_full()`/`process_incremental()` 对 `data["history"]["quarters"]`/`["values"]` 始终用 `.append()` 追加写入，从不排序。当某个投资者先抓近期持仓、后来又补抓了更早的历史 filing（如格伦伯格/Akre 这样 2013 年就开始披露但初次只抓了 2016 年以后的情况），新补抓的早期季度会被追加到数组末尾而不是插入到正确时间位置，导致 `quarters`/`values` 这两个平行数组变成乱序。因为前端 `renderHistoryChart()`、`generateHistoryInsight()`、`renderHistoryMobile()` 都直接按数组原始顺序读取（假设它已经是时间序），乱序不仅让 X 轴标签乱，连折线走势本身都是错的（连接了乱序的数据点），且 AI 摘要文案会彻底错误（修复前实测会说“从 2026 Q1 到 2016 Q1”这种时间倒流的话术）。

**修复**：
1. `fetch_13f_all.py` 新增 `_sort_history_in_place()`，在 `save_data()` 写盘前自动按季度时间顺序重排 `quarters`/`values`（`holdings` 是以季度字符串为 key 的字典，不受数组顺序影响，无需调整）。已正确的数据无副作用（直接 `return`）。
2. `app.js` 的 `renderHistoryChart()` 同步加了一层前端防御性重排（不依赖后端数据必然已排序，即使旧数据文件没重跑抓取也能正确显示）。
3. 写一次性脚本对现有数据文件回填修复，发现并修正了两个受影响文件：`greenberg.json`、`akre.json`（其他投资者文件本来就是时间序，无需改动）。修复后验证：重跑脚本确认幂等（第二次 0 个文件需改动），`holdings` 字典 key 集合与排序后 `quarters` 集合一致。
4. 视觉验证：修复后格伦伯格历史趋势图表 X 轴从 2013 到 2026 按序排列，折线走势变得连贯合理（之前乱序时折线有非理性锯齿状跳变），AI 摘要文案变为正确的“从 2013 Q2 到 2026 Q1，规模增长 87%”。

**经验教训**：
- 对既有回归验证时发现的“无关”问题不要直接忽略——先确认是否是本次改动引入的回归（看 `git diff` 涉及文件），如果确认不是回归而是早已存在的 bug，应该向用户报告并征求处理意见，而不是默许推迟。
- 分批/增量抓取的历史数据，只用 `.append()` 而不排序，在抓取顺序与时间顺序不一致时（先近期后回填早期）会静默产生乱序数组，不会报错但下游消费方会静默得到错误结果。写盘前显式排序比依赖抓取顺序更稳健。
- 同一个根因同时在后端（数据写盘）和前端（渲染读取）修复两道防线更稳健——后端修复让数据文件本身规范，前端修复让已经存在的旧数据文件（还没重跑抓取脚本）也能正确显示。

### 2026-07-26（第四条）：Finnhub 偶发 502 导致参考股价整体消失 + BRK/B 斜杠 ticker 恒久失败

**背景**：用户反馈“谷歌和拼多多的参考股价怎么没有了”。诊断确认是当天 CI 跑数据更新时，Finnhub 报价接口偶发返回 502 Bad Gateway，`GOOGL`/`GOOG`/`PDD` 三个 ticker 拉取失败（同批次 `AAPL`/`MSCI` 等其他 ticker 均成功，说明是接口抖动而非系统性故障）。排查过程中顺带发现另一个长期存在、性质不同的独立 bug：`BRK/B`（伯克希尔哈撒韦 B 类）参考股价一直是 `--`，且与本次 502 事件无关。

**根因 1（502 导致数据消失）**：`fetch_prices_all.py` 的 `finnhub()` 重试逻辑此前只处理 429 限速错误，对 500/502/503/504 等临时性网关错误不重试、直接判定失败。更严重的是，报价拉取失败后的降级逻辑会无条件把 `quotes[tk]` 覆盖成 `{"error": True}`，即使这个 ticker 昨天/上一次 CI 运行时是有效缓存价格的，也会被这次的临时性网络抖动连带清空，导致前端“参考股价”列从有数据变成空白 `--`，看起来像数据“凭空消失”了。

**根因 2（BRK/B 恒久失败）**：与网络抖动无关，是纯粹的字符串处理 bug。13F 原始申报数据里，不同投资者对伯克希尔 B 股的 ticker 拼写不统一——多数用 Finnhub 标准写法 `BRK.B`（代码里已对点号做了 `BRK%2EB` 的 URL 编码特殊处理），但李录、Akre、伯科威茨、段永平等持仓文件里实际存的是斜杠写法 `BRK/B`/`BRK/A`，未命中该特殊处理分支，直接把带 `/` 的字符串拼进请求路径，`/` 被当成 URL 路径分隔符（例如错误请求了 `.../quoteSummary/BRK/B?...`），必然请求失败，报价从未成功过一次。

**修复**：
1. `fetch_prices_all.py` 新增 `_RETRYABLE_HTTP_CODES = {500, 502, 503, 504}`，`finnhub()` 对这些临时性错误按 `2*(attempt+1)` 秒退避重试（沿用已有的 429 处理不变，仍是 `30*attempt` 秒），同时对请求过程中的通用异常也补上同样的重试。
2. 报价拉取全部重试仍失败时，不再无条件覆盖成 `error`，而是检查 `existing_quotes` 里是否有该 ticker 上一次的有效缓存价格，有则回退为 `{**上次缓存, "stale": True}`，没有历史缓存可回退才真正标记 `error`。`app.js` 相应在价格数字旁追加一个 ⏱ 小图标（悬停提示“实时报价暂时拉取失败，显示为最后一次成功报价”），让用户能区分“实时价”和“回退价”，而不是静默展示旧数据当作最新数据、或者粗暴清空成 `--`。
3. 将 `BRK.B`/`BRK/B` 一并归一化编码为 `BRK%2EB`、`BRK.A`/`BRK/A` 一并归一化编码为 `BRK%2EA`，不再要求上游 13F 数据的 ticker 拼写必须是点号格式。后续触发真实 CI 验证时发现 `yahoo_chart()`（估算成本用的历史 K 线）是第三处受影响的独立代码路径——它只把点号写法转成 yfinance 格式（`BRK-B`），未处理斜杠写法，一并补齐归一化。
4. 验证方式：写临时单元测试脚本覆盖（a）持续 502 重试三次耗尽后正确返回 `None`、（b）前两次 502 第三次成功、（c）失败但有缓存时正确回退并打 `stale` 标记、（d）斜杠/点号写法归一化编码结果一致，四类场景全部通过后删除临时脚本；再用 Playwright 分别截图桌面端与移动端两种数据场景（真实 `error` 场景验证 `--` 显示不受影响、构造的 `stale` 场景验证 ⏱ 图标正确渲染无布局问题）；最终手动触发一次真实 CI 全量跑（run 30204636293），确认 `GOOGL`/`GOOG`/`PDD` 当次已正常拉取成功（Finnhub 接口本身已恢复，未触发 stale 分支，但重试/回退逻辑已具备，下次再抖动时会自动生效）。

**经验教训**：
- “数据消失”类问题优先怀疑“成功路径改成了失败路径覆盖旧结果”，而不是“数据源本身没有这个数据”——先查 CI 实际日志里那次运行的具体 HTTP 错误码，不要停留在猜测。
- 降级/重试逻辑的默认行为不应该是“失败就清空”，而应该是“失败就尽量保留已知最新有效值，并诚实标记这是回退值”——对时效性数据（股价）尤其如此，因为“旧数据 + 明确标注”远优于“没有数据”，也优于“悄悄用旧数据冒充最新数据”。
- 同一份数据里出现两种不同写法表示同一实体（`BRK.B` vs `BRK/B`）时，不能假设上游数据格式统一——所有把 ticker 直接拼进 URL 路径/查询参数的地方都要考虑非常规字符（`/`、`.`、空格等）的转义，遇到一个特例做了特殊处理后，要主动 grep 数据文件里是否存在同一实体的其他写法变体，不能只处理眼前报错的那一个。
- 本地沙箱没有第三方 API 密钥时，无法做到端到端真实网络验证，此时单元测试覆盖具体分支逻辑 + 触发真实 CI 观察实际结果，是可行的替代验证路径，但要向用户明确说明“这次 CI 跑没有真正触发新逻辑（因为接口没抖动），逻辑正确性由单元测试保证”，不能含糊地宣称“已完整端到端验证”。

### 2026-08-02：思维试炼（Mind Trials）小游戏从 Vite 打包产物重建为原生 vanilla JS

**背景**：“思维试炼”是嵌在“港股分拆”旁边的一个互动小游戏 tab（改编自查理·芒格 1996 年演讲，玩家扮演 1884 年可口可乐创业者，通过 5 个试炼推导思维模型）。用户反馈移动端卡片内容有裁切、视觉不自然、触摸滑动有 bug。排查发现游戏原始代码从未提交到仓库——只有 Vite 构建后的压缩产物（`game/assets/index-*.js`/`.css`），无法定点修复内部裁切问题，用户明确要求“直接根据内容重新构建”而非继续在压缩包上打补丁。

**根因**：旧版本是不可编辑的第三方构建产物，源码缺失导致任何 CSS/JS 层面的精确修复都不可行，只能整体替换。

**修复**：
1. 从压缩后的 `index-DDmNwwLI.js` 用正则提取出全部 221 条中文剧本片段（序言 + 5 个试炼 + 尾声），从压缩 CSS 提取原始配色（金/绿/赭石/蓝 + 米白底 + 墨色字），复用原有自托管字体（Noto Serif SC + Cormorant Garamond）。
2. 在 `game/v2/` 下用纯 vanilla JS/CSS/HTML 从零重建整个引擎（无框架依赖）：`game.js`（状态机式场景切换：intro→letter→5个试炼→ending，含点选反馈、危险卡翻转、三滑块实时算利润、分类判断、历史对照结局）+ `style.css`（响应式、CSS 变量配色、安全区适配）。`app.js` 的 `GAME_URL` 改指向 `game/v2/index.html`。
3. **过程中发现并修复了一个新版本自身的严重 bug**：`#app` 用 `min-height:100vh` 而 `html,body` 用固定 `height:100%`，导致内容溢出时页面既不整体撑高、内部 `.stage` 也因为 `flex:1` 永远不会真正溢出而触发自己的滚动条——整页“看似正常渲染但完全无法滚动”，试炼四内容较长时底部的“下一试炼”按钮实际不可达。修复：去掉 `html,body{height:100%}` 的固定高度，让 `body` 能随内容自然撑高、交由整页 `document` 滚动；同时移除 `.stage` 上多余的 `overflow-y:auto`（该设置在内容未真正溢出 `.stage` 自身时会形成一个“判定为可滚动却从不触发滚动”的死滚动层，阻断了事件正确上抛给外层 `document`）。
4. 用真实 CDP 触摸事件模拟（`hasTouch:true, isMobile:true` context + `Input.dispatchTouchEvent`）在移动端视口验证：修复前整页滑动 `scrollY` 纹丝不动；修复后滑动可达底部按钮，且拖动 `<input type=range>` 滑块的触摸交互正常、数值与视觉实时同步。
5. 在真实 `app.js` 的 iframe 嵌入场景下（点击“思维试炼” tab 后实际渲染的 iframe，而非直接打开 `game/v2/index.html`）额外测试触摸滑动是否会“泄漏”到外层父页面滚动而不是滚动 iframe 内部——这是最初 bug 报告的核心场景。多组对照实验（简单最小复现结构 vs 真实深层 DOM 嵌套结构、wheel 事件 vs CDP 合成触摸事件、加长 `touchStart`→`touchMove` 间隔）显示：问题只在“深层 DOM 嵌套 + CDP 合成触摸手势 + 不加人工延迟”的组合下稳定复现，wheel 事件在同样深层嵌套下完全正常，且给触摸序列加timing 余量后泄漏程度显著降低但不为零——这与 Chromium 处理跨 frame（OOPIF）`touch-action` 协商的已知超时机制吻合，更像是无头自动化环境合成触摸手势的时序局限，而非可用 CSS 完全消除的产品 bug。已将外层 iframe 容器的 `touch-action` 从最初的 `none` 调整为 `pan-y`（W3C 推荐的、允许纵向手势同时仍支持跨 frame 协商的取值），并保留 `overscroll-behavior:contain`，作为力所能及的防御性设置；**这一项没有在真机上做最终确认，建议用户实机验证一次**。
6. 确认旧 `game/index.html`、`game/assets/`、`game/fonts/` 无任何残留引用后整体删除。

**经验教训**：
- 第三方框架构建产物（压缩后的 Vite/webpack bundle）一旦源码丢失就无法再做定点修复，只能整体重写替换——这类小工具/小游戏应该从一开始就把源码（未压缩版本）一起提交仓库，构建产物本身不该是唯一留存形态。
- flex 布局里 `flex:1` 的子元素设置 `overflow-y:auto` 是一个容易被忽视的陷阱：只要父容器允许自身随内容“生长”（如 `min-height` 而非固定 `height`），这个子元素永远不会真正溢出，`overflow:auto` 形同虚设，且会掩盖“page 本该可以整体滚动却完全不能滚动”的真实问题——排查“滑动无效”类 bug 时要检查完整的滚动责任链（谁的 `scrollHeight > clientHeight`），不能只看某一层的 CSS 属性是否“看起来对”。
- 自动化触摸手势测试（CDP `Input.dispatchTouchEvent`）在扁平/浅层 DOM 结构下高度可靠，但在深层嵌套 + 跨进程 iframe 场景下可能因为时序过快而无法完全复现真实设备的触摸滚动行为；发现这类差异时应该用 wheel/编程式 `scrollTo` 等无时序依赖的方式交叉验证，区分“真实产品 bug”与“测试工具局限”，并如实告知用户哪些结论已经过充分验证、哪些仍需要真机确认，不能笼统地宣称“移动端触摸已完全验证”。

### 2026-08-02（第二条）：思维试炼内容补全（社会认同效应）+ 单位本地化（盎司→毫升）+ 移除假声音按钮

**背景**：用户体验完 v2 重建版后反馈三点：(1) 顶部“声音”按钮点了没反应；(2) 游戏改编自芒格 1996 年演讲，问是否有原演讲内容被遗漏；(3) 试炼中“64 盎司/8 杯”水消费量的单位对中文用户不直观，建议换算成毫升。

**根因**：
1. **假声音按钮**：`renderChrome()` 里的 `sound-btn` 只是切换一个 emoji（🔈↔🔊）+ 触发一次震动反馈，全仓库 grep `audio|sound|\.mp3|\.wav|play()` 均无真实音频对象或播放调用——从上线第一天起就是纯装饰性死代码，从未播放过声音。
2. **内容遗漏**：通读原演讲多个中文转译来源确认，芒格明确提出品牌是通过**三种**心理机制建立条件反射——操作性条件反射、巴甫洛夫条件反射、以及**社会认同（Social Proof，因为看到别人都在用而产生的从众心理）**。游戏试炼四的“条件反射分类”环节上线时只做了前两种的二选一分类，完全没有覆盖社会认同这一支柱。
3. **单位问题**：试炼一结果文案与试炼三注释里的“64 盎司（8 杯）”水消费量，直接沿用了英文原演讲的美制单位，未做本地化换算。

**修复**：
1. 试炼四 `T4_ITEMS` 从 6 项扩充到 8 项，新增“邻桌都在喝”“随处可见的红色标志”两张社会认同场景卡；选择器从二选一（操作性/巴甫洛夫）改为三选一（操作性/巴甫洛夫/社会认同），标题与引导文案同步更新为“三重心理效应”。
2. 全部“64 盎司（8 杯）”文案换算为“1.9 升（8 杯，每杯约 237 毫升）”（1 液体盎司 ≈ 29.5735 ml，64oz≈1893ml，8oz≈237ml），保留美分单位不变（用户明确要求只换算液体盎司，美分作为“每份净赚”的小额利润单位对国内用户也是直觉单位）。
3. 移除 `renderChrome()` 里的 `soundBtn` 元素及其 `onclick`、`state.soundOn` 字段、CSS `.sound-btn`/`.sound-btn:active` 规则——彻底删除而非补充真实音效实现（补音效需要新增音频素材，超出当次反馈范围，且诚实地去掉无效交互优于保留一个看起来能点但什么也不做的按钮）。
4. 结局的“真实历史对照”环节保持不变（用户明确表示不需要补充“预测 vs 实际后续表现”这段内容）。
5. 本地起 dev server，用 Playwright 在桌面端（1280×900）与移动端（390×844，`isMobile:true, hasTouch:true`）分别走完整流程截图验证：确认顶栏声音按钮消失且布局未留空隙、试炼一结果文案正确显示“1.9 升”而非“盎司”、试炼四 8 张卡片正常两列布局、点击后三选一选择器完整显示无裁切。

**经验教训**：
- 反馈“功能没反应”时，第一步应该 grep 相关关键词确认是真 bug 还是从未实现过的装饰性代码——如果是后者，删除比继续假装它能工作更诚实，也符合“清理无用代码”的一贯要求。
- 改编真实演讲/文本内容的教育类产品，做“内容是否遗漏”自查时要对照原始多个转译来源逐条核对结构性框架（如“三种心理机制”这种明确列举的清单），不能只凭大致印象判断“应该都覆盖到了”。

### 2026-08-02（第三条）：序言铺垫五个思维观念 + 试炼内容深度扩充（两个大问题/可得性/lollapalooza 四驱动力）

**背景**：用户认可了前两轮修复后，进一步反馈整体内容“仍然有些简略”，指出芒格 1996 年演讲开头提到的“五个有用的思维观念”没有在游戏里作为铺垫出现，且演讲里还有大量未被采用的细节，希望用巧妙方式融入。

**方案确认**：通过 `ask_user_question` 确认两个关键决定——(1) 五个思维观念融入现有序言卡片（不新增屏幕）；(2) 除了铺垫，适当新增交互小环节来容纳演讲细节（而不是全部堆进台词文案）。

**具体内容扩充**：
1. **序言（`renderLetter`）**：新增 `LENS_DEFS` 数据（化繁为简/数学思维/逆向思维/跨学科基础智慧/鲁拉帕路萨效应），以新建的 `.lens-list`/`.lens-item` 样式展示为图标+标题+一句话说明的卡片列表；序言文案同时补充“格罗兹慈善基金会”捐赠细节与芒格开场自嘲“我先讲个虚构的故事——别当真”的口吻。
2. **试炼一**：正确/错误反馈后新增“两个显而易见的大问题”（商标保护强度＋先打下一座城市再谈全球扩张），用 `T1_BIG_QUESTIONS` 数据驱动，复用 `.lens-item` 样式；同时在结果文案里补充“可得性”要点（未经试用的竞品很难取代已形成习惯的产品）——这是原演讲中操作性条件反射部分之前一直没被采用的点。
3. **试炼三**：滑块结果之后新增 lollapalooza 四大驱动力交互小环节（货币贬值/购买力提升/人均液体摄入量提升/技术降本），用 `T3_LOLLA_ITEMS` 数据驱动，复用 `.tile-grid` 点击展开模式（与试炼四相同交互，但不强制点完才能继续，作为可选延伸探索，不阻塞主线节奏）。
4. **试炼五**：正确选项的详情文案末尾补充一段“五个试炼串起来看”的 lollapalooza 点题，与序言、尾声前后呼应。
5. **尾声**：“双重条件反射”表达同步更新为“三重心理效应”（与试炼四已有的社会认同保持一致）；新增一段与序言自嘲首尾呼应的收尾文案（“开头那个‘格罗兹’的故事是虚构的——但你刚刚练习的这五个思维透镜，可一点不虚”）。
6. 新增 CSS 类 `.lens-list`/`.lens-item`/`.lens-icon`/`.lens-name`/`.lens-desc`，与现有 `.card`/`.tile-grid` 体系保持视觉一致。
7. 本地起 dev server，用 Playwright 在桌面端（1280×900）与移动端（390×844，`isMobile:true, hasTouch:true`）分别跑完全流程（序言→试炼一~五→尾声）截图，逐张亲自查看确认新内容无裁切、无重叠、排版整齐。

**经验教训**：
- 中文输入法/编辑工具的自动纠错会在未明确提示的情况下改掉原文字词（如“雅可比”被改成“雅可宾”、“拍胸脆”被改成“拍胸脉”/“拍胸脆”、“陷进”被改成“阁进”）——新增/修改中文文案后，必须在 Playwright 实际渲染的截图上逐字核对文本，不能只看源码字符串——因为源码本身在写入时就可能已经被编辑工具悄悄地改字。
- 内容扩充优先选择“融入现有台词/结果文案”而不是“新增独立屏幕”，可以在不拉长游戏节奏的前提下大幅提升信息密度；适当新增的交互小环节（如 lollapalooza 四卡片）不设为必答项，避免拖慢主线。


### 2026-08-02（第四条）：主站"关于李录"介绍文字"闪一下就消失" bug（芒格评价被 JS 覆盖掉）

**背景**：用户反馈主站首页"关于李录"模块在页面刚打开时会先短暂显示一段包含芒格对李录评价的文字，然后"闪一下"这段文字就消失了。

**根因**：`index.html` 里 `#aboutText` 的静态初始内容（页面源码里写死的默认文案）确实包含芒格那句评价——"芒格曾公开赞誉：'李录是一个天才，我们一起赚了很多钱。'"。但页面加载完成后，`DOMContentLoaded` 会触发 `initApp()` → `switchInvestor('lilu')` → `updateInvestorContent()`，该函数里李录默认分支（`else` 分支）用 `rt.innerHTML = ...` 写入了一个更早、更简略的版本——不包含芒格评价，也不包含"三学位"细节——把静态 HTML 内容整段覆盖掉了。两段文案一直存在分歧（可能是重构成 `investors.json` 单一数据源时，只同步了投资者列表配置，没同步检查 `updateInvestorContent()` 里手写文案与 HTML 静态内容是否一致），用户因此每次开页都会看到"先有评价，闪一下就没了"的闪烁。

**修复**：将 `app.js` 中 `updateInvestorContent()` 里李录默认分支（中文+英文两个分支）的文案改为与 `index.html` 静态内容完全一致：补回"1989 年赴美""哥大罕见的'三学位'成就者""芒格曾公开赞誉：'李录是一个天才，我们一起赚了很多钱'""关注人权、教育和救灾"等被省略的细节，英文分支同步补齐对应翻译。本地起 dev server 用 Playwright 模拟页面完整加载流程（包含 `DOMContentLoaded` 触发后的 JS 覆盖），直接取 `#aboutText` 最终渲染后的 `innerText`，确认中英文两个语言下都完整包含芒格评价且不再发生"加载后消失"。

**经验教训**：
- 静态 HTML 预渲染内容与 JS 运行时动态覆盖内容（如多语言/多投资人切换需要的 `innerHTML` 重写）必须保持内容一致，否则用户在页面刚加载的瞬间会看到"内容闪现又消失"的闪烁——这类 bug 很难仅通过看最终渲染结果发现，必须同时对比静态源码与 JS 运行时覆盖逻辑。
- 修复这类"两个数据源存在分歧"的 bug 时，需要确认最终以哪个为准（本次以内容更丰富的静态 HTML 为准），而不是随意两边取一个。

### 2026-08-04：分拆追踪器"新进展"徽章误标几乎所有条目 + 郭海庆持股面板口径说明缺失

**背景**：用户反馈两个问题：(1) 分拆（spinoff）追踪器里几乎所有条目都显示红色"新进展"徽章，但实际并没有新公告；(2) 香港中旅（00308.HK）大股东郭海庆的增减持面板数字与用户在其他地方查到的数字"对不上"。

**根因**：
1. 分拆追踪器的 `_spinoffIsNew()` 判断逻辑本身没有问题（lastUpdated 距今 ≤ 7 天就算"新进展"），问题在于数据层：`spinoff_price_refresh.py` 的 `tag_last_updated()` 对比 git HEAD 旧版本与当前文件时，没有把 `lastPrice`（实时行情报价，接口偶发失败会在 `null` 与数值之间跳变）、`marketCap`/`parentMarketCap`（每日随股价波动）、`greenblattNote`（由上述数值推导生成的文案，数值微调就跟着变字）这些每次运行都会自然波动的字段排除在 `IGNORE_KEYS` 之外，导致几乎每次 CI 跟批（每日两次）都把几乎所有条目误判为"有实质变化"，重新打上今天的时间戳。用真实历史提交（`cf80638~1` → `cf80638`）验证，确认 `02161.HK` 等条目的 `lastPrice` 确实在相邻两次跟批里从 1.92 变成 `null`，而公告内容本身根本没变。
2. 郭海庆持股面板：本地 `guo_haiqing.json`（7.00%、387,596,000 股，截至 2025-05-23）与多个官方/三方数据源（etnet、HKEX 招股书、Investing.com 香港等）比对均一致，并无更新披露。真正的"对不上"来自口径差异：据 HKEX 招股文件，郭海庆 7.00% 的权益由两部分合并而成——个人名义持股约 5.09%＋全资子公司 Surpassing Investment Limited 持股约 1.91%，部分第三方数据平台（如同花顺）会把两者拆开列示，而面板只显示合并后的 7.00%，且没有说明口径，容易让对照过其他源的用户以为数据错误。

**修复**：
1. `spinoff_price_refresh.py` 的 `tag_last_updated()` 把 `lastPrice`、`marketCap`、`parentMarketCap`、`spinoffPricePerf`、`greenblattNote` 全部加入 `IGNORE_KEYS`，只有 `announcements`/`_status`/`summary` 等真正反映内容变化的字段发生变化时才会重新打 `lastUpdated`。
2. `app.js` 的 `_spinoffIsNew()` 改为优先用公告本身的日期判断（港股用顶层 `latestDate`，美股无该字段时从 `announcements` 数组取最大日期，因为该数组不保证总是按日期降序），不再直接依赖 `lastUpdated`。
3. 写一次性脚本回填 `spinoff.json`/`spinoff_us.json` 里已被污染的 `lastUpdated`，重置为每条真实的最新公告日期，避免历史脏数据继续误导。修复后验证：港股 34 条中 0 条显示"新进展"，美股 64 条中只有 1 条（最近8天内确实有新公告的 VREOF）显示，与真实公告情况完全匹配。
4. 郭海庆面板底部加一行口径说明，明确告知 7.00% 是个人名义 + Surpassing Investment Limited 合并后的总额，部分第三方源会拆开展示，两者都正确。

**经验教训**：
- 基于 git diff 判断"是否有实质进展"时，必须把所有会因为外部行情/市值波动而自然变化的字段都显式排除，否则会把"没有新内容，只是价格变了"误判为"有新进展"。由 LLM 或规则函数推导生成的文案字段尤其危险——它们看似是"内容"，实际上只是上游数值的文字投影，不应误认为真正的内容变化。
- "新进展"这类语义判断应该直接绑定到最能代表该语义的字段本身（如公告日期），而不是依赖一个中间加工出来、可能因为其他逻辑 bug 而出错的转换字段，这样即使后端打标逻辑再次出错，前端展示也不会跟着错。
- 用户报告的"数据对不上"未必是真正的数据错误，也可能是不同数据源采用了不同的统计口径（如个人 vs 个人+受控法团合并）。遇到这种情况应在界面上直接说明口径，而不是假设哪个数字错了去改数据。

### 2026-08-04（第二条）：港股分拆大股东历史增减持面板——从"仅郭海庆一家"扩展到 20 家公司的全自动化数据管道

**背景**：郭海庆（00308.HK 香港中旅）的大股东增减持追踪面板此前是手工构建的孤例（`guo_haiqing.json`）。用户要求：把同等丰富度的历史增减持记录扩展到全部 98 家分拆标的中所有能识别出个人/家族大股东的公司，港股 34 条先做，且必须全自动化，不允许靠人工补丁或 LLM 猜测数值。

**方案**：
1. 复用 HKEX 披露利益（DI）系统的两个端点：`NSSrchCorpList.aspx`（按股票代码查内部 `sid`）→ `NSNoticeSSList.aspx`（`sid` 对应的完整历史通知记录，8 列：表格编号、股东名、原因代码、买卖股数(L)、均价、变动后股数(L)、变动后比例(L)、事件日期）。
2. 先在港股 34 条里筛出"有可识别个人/家族大股东"的公司，排除大股东是机构法人（如中信证券全资子公司）的条目，得到 20 家。
3. 写 `fetch_shareholder_history.py`，对每家公司自动抓取完整历史通知记录，输出统一 schema 到 `spinoff_shareholder_history.json`。

**过程中发现并修复的四个 bug**（均在数据管道内部，不涉及任何手工填值）：
1. **原因代码前缀取错位数**：HKEX 原因代码是 4 位数字（如 1101、1210、1701），分类应该取**前 2 位**（10/11/12/13/17）对应首次披露/增持/减持/性质变动/百分比漂移五大类，早期实现误取前 3 位，导致大部分记录分类失败或归错类。
2. **未签名字段方向丢失**：HKEX 原始"买卖股数"列本身不带正负号，减持事件必须手动把该数值转成负数，否则前端会把减持显示成增持同等的正数变动。
3. **当前持股快照选人规则错误**：多个股东并存时，此前用"取比例最高的一条"作为当前持股快照，正确规则应为"取 `lastNoticeDate` 最新的一条"——比例最高不代表最新。
4. **同一事件重复出现**：同一笔 DI 事件有时会在两个不同的表格编号前缀（IS/DA）系列下各出现一条完全重复的记录，需要按 (日期, 股数, 比例) 三元组去重。

**本轮（第二条内新增，第五个 bug）**：验证过程中发现"百分比变动（非交易）"和"性质变动（方向不明）"这两类事件的 `change` 字段仍然被错误填入数值——以 01523.HK 珩湾科技为例，2022-10-26 那条"比例漂移"记录的 `change=756,000,000` 恰好等于其"变动后股数"字段的值，明显不是真实的买卖股数，而是把"股数"错当"变动量"填了进去。代码里原本有一行注释声称"这两类事件已经处理为不填变动量"，但对应的实现代码根本不存在——注释描述了意图，代码没有跟上。修复：在 `fetch_shareholder_history.py` 里显式添加逻辑，当 `event_type` 属于这两类时强制把 `change` 设为 `None`，前端相应显示为"—"，不再展示误导性数值。这是本次坚持"不确定就不填"原则的又一例证。

**前端集成**：`app.js` 新增 `_loadShareholderHistoryData()`（拉取并缓存 JSON）、`_shareholderEventMeta()`（把五种事件类型映射为图标+颜色+中英文标签：增持▲绿/减持▼红/首次披露●金/性质变动○灰/百分比漂移·灰）、`_renderShareholderHistoryPanel()`（复用郭海庆面板的视觉样式：SVG 折线图+当前持股百分比+逐条记录表格），在 `renderSpinoff()` 里按 `stockCode` 匹配自动挂载到对应分拆卡片下方，不需要为每家公司单独写渲染代码。

**验证**：本地 Playwright 分别在桌面端（1280×1000）与移动端（390×844，`isMobile:true, hasTouch:true`）对珩湾科技（01523）、理士国际（00842）、新城發展（01030）、鹰君（00041）四家代表性样本展开面板截图，逐张亲自用 `read` 查看确认：增持/减持/性质变动三种事件的颜色和箭头方向区分清晰，负号变动值正确对应减持记录，SVG 折线图和百分比标注不重叠不溢出，移动端换行正常、面板标题与数据表格完整可见不裁切。另跑了一遍全量字段完整性检查（20 家公司的 `shareholder`/`current_pct`/`current_shares`/`last_updated` 均非空，且所有历史记录的 `shares` 字段均非零非空），确认无遗漏字段导致的潜在渲染异常。02196.HK 复星医药（大股东郭广昌）经确认为真实数据缺口——HKEX 通知记录本身为空，非抓取逻辑问题。

**经验教训**：
- 代码注释声称"已处理"某种边界情况时不能直接信任，必须去看对应的实际实现是否存在、逻辑是否真的覆盖了该分支——本次的"性质变动不填变动量"就是一个注释与代码脱节的真实案例。
- 分类型/枚举型的原始编码字段（如 HKEX 的 4 位原因代码）在做前缀截取分类时，必须先去官方文档核实清楚"分类粒度对应几位前缀"，不能凭直觉猜测位数，1 位之差会让分类系统性失效。
- 涉及"总量快照选取"的规则（如"当前持股取哪一条记录"），永远优先信任时间戳而非数值大小本身——数值最大/最高不等于最新。

